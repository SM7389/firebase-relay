{
  "name": "firebase-relay",
  "version": "1.0.0",
  "description": "Relay HTTP data from SIM800L to Firebase over HTTPS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": "18.x"
  }
}
